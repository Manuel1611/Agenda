<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.agenda">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Agenda.NoActionBar">
        <activity android:name=".view.MainActivity" />
        <activity android:name=".view.ActivityDatosUpdate" />
        <activity android:name=".view.ActivityUpdate" />
        <activity android:name=".view.ActivityDelete" />
        <activity android:name=".view.ActivityInsert" />
        <activity android:name=".view.ActivityInicio">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>